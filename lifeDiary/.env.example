# Django
DJANGO_SECRET_KEY=replace-with-strong-random-key

# Database (production)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=6543

# Email (production Resend API; dev uses console backend)
# Live recovery email delivery requires a purchased/configured sender domain verified in Resend.
RESEND_API_KEY=
DEFAULT_FROM_EMAIL=LifeDiary <noreply@your-verified-domain.com>
