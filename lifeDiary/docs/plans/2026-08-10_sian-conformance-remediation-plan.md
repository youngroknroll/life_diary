# 시안 정합 재작업 계획 (A+B+C)

기준 시안: claude.ai/design 프로젝트 `a76fc6a6-816e-4865-a42d-d673ab2bca93`
파일 `Life Diary P0 시안.dc.html` (22화면). 2026-08-10 원본 전수 대조.

선행 문서 — `docs/plans/2026-08-01_sian-screen-inventory.md`,
`docs/refactoring/2026-08-01_p0-sian-redesign.md`. 두 문서의 "완료" 표기는
아래 항목만큼 과대 기록이었다. 이 계획이 그 격차를 닫는다.

## 승인된 이탈 (재작업 대상 아님)

주 기간(달력 주) · 카테고리 색(파스텔) · 잉크 `#0F1A14` · 라벨 규칙 ·
홈 kicker · 데스크톱 슬롯 19px · 7c 요약 탭 내장 · 7d 인라인 폼 대체 ·
로그인 후 홈 유지. 근거는 선행 두 문서에 있다.

## 진행 (2026-08-10)

| 단계 | 커밋 | 상태 |
|---|---|---|
| 1 | `b2ddbfc` · `f9cb8fc` | 완료. 두 리뷰어 판정 반영 |
| 2 | `de9d42c` · `2399df4` | 완료. 두 리뷰어 판정 반영 |
| 3 | `9385a5c` + 보완 | 완료. 두 리뷰어 판정 반영 |
| 4~7 | — | 미착수 |

계획에 없던 시안 불일치를 두 건 더 찾아 함께 고쳤다 —
**카테고리 표시 순서**(DB 가 시안과 정반대)와 **데스크톱 태그 합계**(시안 3d
가 유지하는데 지웠던 것). 둘 다 격차 목록 초판에 없었다.

## 격차 목록

### A. 구조 불일치

| # | 화면 | 시안 | 현재 | 백엔드 |
|---|---|---|---|---|
| A1 | 6a 태그 관리 | 행 리스트 `이름‧누적 기록`, 카테고리 그룹, 인라인 편집, 행 드래그, 카테고리 패널 | 카드 그리드, 모달 편집, 정렬·패널 없음 | `Tag.display_order` 신설 + 정렬 API |
| A2 | 5c·6b 설정 | 목표 최상단 인라인 표, 태그·카테고리 칩 노출, 계정(아이디·비밀번호 변경·탈퇴) | 목표 3번째 2열 폼, 태그는 링크, 비밀번호 변경 없음 | 없음 |
| A3 | 6e 온보딩 | STEP1 태그 선택, STEP2 실제 저장, STEP3 스테퍼+미리보기 | 정적 칩, `aria-hidden` 데모, select 3개 | STEP1 선택 저장 · STEP2 슬롯 저장 |
| A4 | 7b 태그 모달 | 이름→카테고리, 카테고리 5카드, 최대 12자, "태그 만들기" | 카테고리→이름, select, 50자, "저장" | 이름 길이 규칙 |

### B. 구성요소 누락

| # | 화면 | 누락 | 백엔드 |
|---|---|---|---|
| B1 | 3d·4d 월 탭 | 주차별 요약 표 (시안은 태그 합계를 뺀 자리) | 주차 버킷 집계 |
| B2 | 3b 일 탭 | 하루 구성 표의 `슬롯`·`7일 평균 대비` 열 | 집계 확장 |
| B3 | 6c 기록 빈 상태 | 자주 쓰는 태그, 미래 시간 구분 | 사용빈도 정렬 · 달성률 분모 |
| B4 | 7a 카테고리 설명 | 예시 태그 칩 열 | 카테고리별 예시 태그 |
| B5 | 6f 로그인 | 남은 시도 횟수, 필드 인라인 오류 | 뷰 컨텍스트 |
| B6 | 6f 회원가입 | 약관 동의 체크박스, 라벨 "아이디" | 폼 필드 |

### C. 스킨 잔재

C1 `btn btn-primary`·`btn-outline-*` → `btn-sian` (signup, mypage, 태그 카드)
C2 `mypage.html`·`usergoal_list.html` 인라인 `<style>` 제거
C3 `password_reset_*` 4종이 옛 스킨(`card-header` + `btn btn-primary` +
`text-danger small`)에 남아 있다. 1단계에서 신설한 비밀번호 변경만 새 스킨이라
**로그인 화면이 한 링크 건너 두 벌의 디자인 언어로 갈린다.** 1단계 범위 밖이라
미뤘고, 2단계에서 처리한다. (사후 리뷰 지적)

## 단계

각 단계는 독립 커밋이고, 백엔드가 붙는 단계는 Red-Green을 먼저 돈다.

1. **C + B5 + B6** — 스킨 잔재, 로그인·회원가입. 위험 낮음
2. **B4 + B2 + B1 + C3** — 카테고리 예시, 일 탭 열, 월 탭 주차별 요약,
   비밀번호 찾기 계열 스킨 통일
3. **A4 + A2 + D3** — 태그 모달, 설정 재구성, 이름 10자 제한
4. **D1** — 기본 태그 폐지. 모델·마이그레이션·관리자 경로 정리.
   되돌릴 수 없으므로 단독 커밋으로 두고 사전 백업을 확인한다.
   **착수 보류 — 운영 DB 백업 확인 대기(2026-08-10).** 백업이 불확실하면
   5단계를 먼저 돌린다. 6·7단계만 순서가 밀린다
5. **D2** — 달성률 분모를 지난 시간 기준으로, 룰 기반 피드백 제거
6. **B3 + A3** — 빈 상태, 온보딩 (슬롯 저장 공유). D1·D2에 의존
7. **A1** — 태그 관리 재작성 + `display_order` + 정렬 API. D1에 의존

4~7단계는 백엔드 계약이 바뀌므로 Backend TDD Cycle을 따른다. 1~3단계는
템플릿·CSS 범위라 프론트엔드 정책만 적용된다.

## 확정된 결정 (2026-08-10 사용자)

### D1. 기본 태그 개념을 없앤다

`Tag.user=null` + `is_default=True`로 전 사용자가 공유하던 기본 태그를
폐지하고, 모든 태그를 개인 태그로 만든다. 가입 시 시드 태그 집합을 개인
태그로 생성한다.

이유 — 시안 6a는 행 드래그 정렬과 인라인 이름 편집을 요구하는데, 공유
객체에는 사용자별 순서와 이름을 적을 수 없다. 채택 집합 표를 두어도 결국
순서·이름까지 그 표가 떠안게 되어 복제와 데이터량이 같아진다.

파급 —
- `Tag.user` 가 non-nullable 이 되고 `is_default` 컬럼이 사라진다
- `unique_default_tag_name` 제약 제거, `unique_user_tag_name` 만 남는다
- 기존 데이터 마이그레이션: 공유 기본 태그를 사용하던 사용자별로 복제한 뒤
  공유 행을 삭제한다. `TimeBlock` 참조를 복제본으로 옮겨야 하므로 되돌릴 수
  없는 마이그레이션이다 — 사전 백업 필요
- 관리자 전용 경로(`is_superuser` 분기, 기본 태그 배지·보호 로직)가
  `views.py`·`tag.js`·`_tag_modal.html`·`tags/index.html`에서 정리된다
- **시안 이탈 신설**: 시안 6a의 `[기본]` 배지가 사라진다

### D2. 달성률 분모는 지난 시간 기준

시안 6c대로 아직 오지 않은 시간을 분모에서 뺀다. 함께 **현재 적용된 룰
기반 피드백을 제거한다**(사용자 결정). `apps/stats/life_feedback.py` 와
그 호출부·템플릿·테스트가 정리 대상이다.

### D3. 태그 이름 최대 10자

`max_length=50` → 10. 설명이 필요한 경우는 기록 저장 시의 메모로 갈음한다
(사용자 결정). 시안 7b의 "최대 12자"와 갈리는 지점이다.

기존 10자 초과 태그 처리 방침을 마이그레이션에서 정해야 한다 — 절삭 시
`unique_user_tag_name` 충돌 가능성이 있으므로 충돌 해소 규칙이 필요하다.

## Frontend Review Evidence

**리뷰 깊이: High.** 정보구조(설정 재편), 모달, 드래그 상호작용, 온보딩
플로우, 페이지 간 공통 패턴을 모두 건드린다.

이 계획은 사전 산출물이 아직 비어 있다. 아래 두 항목이 채워지기 전에는
Frontend Implementation Engineer가 편집을 시작하지 않는다.

### Web Experience Designer 사전 명세 (1~3단계) — 반려 4건 반영본

명세 전문은 세션 로그에 있다. 아래는 **확정 결정과 충돌해 반려한 4건**과
그 대체 지침이다. 나머지 명세는 그대로 채택한다.

| # | 명세 권고 | 반려 사유 | 대체 |
|---|---|---|---|
| R1 | `+ 목표 추가`를 7d 모달로 | 승인된 이탈 "7d는 인라인 폼으로 갈음, 화면 하나 더 만들지 않음"과 충돌. 코드베이스에 목표 모달 없음(확인) | 인라인 추가 행 유지 |
| R2 | 7a 예시 칩을 기본 `Tag` 레코드에서 조회 | D1이 공유 기본 태그를 없앤다 | 정적 큐레이션 문구. 안내 페이지의 예시일 뿐 사용자 데이터가 아님 |
| R3 | 5c 태그 칩에 `기본` 배지 | D1로 개념 소멸 | 배지 제거 |
| R4 | 계정 카드에 `비밀번호 변경 ›` 행 | 인증 상태 비밀번호 변경 라우트가 없었음 | **D4로 신규 구현**(사용자 지시). 행 유지 |

### D4. 비밀번호 변경 기능 신설 (2026-08-10 사용자)

로그인 상태에서 현재 비밀번호를 확인하고 새 비밀번호로 바꾸는 흐름이 없다.
기존 `password_reset_*`은 비로그인 상태의 찾기 흐름이라 대체가 안 된다.
Django `PasswordChangeView` 계열을 쓰되 이 저장소의 인증 화면 스킨
(`auth-panel`, `btn-sian`, 필드 인라인 오류)을 따른다. 1단계에 포함한다.

채택한 핵심 지침 —

- **5c 섹션 순서**: 목표 시간(인라인 표) → 태그와 카테고리(칩 + 5색 범례 +
  "태그 카테고리 설명 보기 ›") → 표시·계정 2단 그리드. 하단 단독 "계정 탈퇴"
  카드는 없애되 **15일 유예 안내 문구는 삭제하지 말고 탈퇴 확인 페이지로 옮긴다.**
- **목표 표 열 축소**: `태그 | 기간 | 목표 | 삭제`. 달성률·실제 기록은 분석
  탭 소관이라 설정에서 뺀다(현행 `_goal_table_section.html`은 6열).
- **A1 미구현으로 인한 잠정 이탈**: 칩 클릭 편집은 A1(7단계)이 있어야 성립하므로,
  그때까지 칩은 읽기 전용으로 두고 `태그 관리 ›` 보조 링크를 남긴다.
- **B2 모바일**: 시안 4b의 하루 구성은 3열을 유지한다. 슬롯·7일 평균 대비는
  360px에서 숨긴다. stats는 단일 템플릿 + `@media (max-width: 575.98px)`
  구조이므로(확인) 템플릿 분기가 아니라 CSS로 처리한다.
- **B1 모바일**: 4d는 5열 표가 아니라 `.settings-row` 계열 스택 리스트.
- **B1은 추가가 아니라 교체**: 월 탭의 기존 "태그 순위" 표를 "주차별 요약"으로
  바꾼다. 상단 선그래프는 유지한다.
- **B1 집중 열은 고정 문구가 아니다**: 시안의 "집중"은 태그 이름일 뿐이므로
  열 머리글을 사용자의 상위 태그 이름으로 채운다.
- **A4**: 이름→카테고리 순, `maxlength="10"`, 도움말 "최대 10자", 카테고리는
  네이티브 라디오 5개(`.category-picker`), 제목 "새 태그", 버튼 "태그 만들기".
- **C2 범위 확대**: `mypage.html` 외에 `usergoal_list.html`에도 `<style>`
  블록이 있다(2~18행). 같은 패스에서 제거한다.
- **i18n**: 신규 문구는 `{% trans %}`/`{% blocktrans %}`로 감싸고 한국어
  `msgstr`를 반드시 채운다(이 저장소의 알려진 fuzzy 회귀 위험).

### Browser Interaction Reviewer 사전 기준 (1~3단계)

**저장소 전역 격차 — 먼저 해결한다.** `.data-table`에 반응형 래퍼가 코드베이스
어디에도 없다. B1·B2·A2가 각각 표를 늘리거나 새로 만들므로, 세 곳이 제각기
다른 방식으로 넘침을 막기 전에 **공용 `.table-scroll` 유틸리티를 먼저 만들고**
셋 다 그것을 쓴다. 수용 게이트는 360px에서
`document.documentElement.scrollWidth === clientWidth`.

**B5 로그인 실패** — 실패 응답에서 포커스가 정정할 입력으로 이동해야 한다
(현재 `views.py`가 재포커스를 하지 않아 문서 최상단에 남는다). 오류 노드는
`aria-describedby`로 입력과 연결하고 입력에 `aria-invalid="true"`를 건다.
"2회 남음"은 **오류 메시지와 같은 노드**에 넣어 한 번만 낭독되게 한다
(별도 live region 금지). 전체 리로드 방식이므로 오류 자체에는 live region이
불필요하다. reCAPTCHA 노출 상태와 인라인 오류가 360px에서 겹치지 않아야 하고,
성공 로그인 시 잔여 오류·`aria-invalid`가 남지 않아야 한다.

**B6 회원가입** — 동의 체크박스는 `label for` 연결, 탭 순서는
아이디→이메일→비밀번호→확인→동의→제출. 미체크 시 클라이언트·서버 양쪽에서
차단하고 포커스를 체크박스로 옮긴다. 라벨 개명 시 `for`/`id` 쌍 유지.
기존 아이디·이메일 중복 확인의 경쟁 조건(stale 응답 폐기)이 깨지지 않아야 한다.

**B4 카테고리 예시 칩** — 읽기 전용이므로 클릭 핸들러·`tabindex="0"`를 달지
않는다(포커스 가능하지만 아무 일도 안 하는 요소는 키보드 함정). 360px에서
3열 그리드가 넘침 없이 접혀야 하고, 대비는 실제 렌더된 조합으로 확인한다
(가장 불리한 `#C99A2E` 기초 생활시간부터).

**A4 카테고리 5카드** — 단일 `role="radiogroup"` + 그룹 라벨, 각 카드
`role="radio"` + `aria-checked`. **roving tabindex**로 그룹 전체가 탭 정지
하나여야 한다. 방향키 이동만으로 선택이 따라가야 하고(Enter/Space 불필요),
포커스 링과 선택 상태를 시각적으로 구분해야 한다. 색 미리보기는 클릭뿐 아니라
**방향키 이동마다** 갱신되어야 한다. Bootstrap 모달 안에 유지해 포커스 트랩·
Escape·백드롭·트리거 복귀를 그대로 상속받는다(커스텀 오버레이 금지).
편집 모드 재진입 시 해당 카드가 선택·포커스 가능 상태로 복원되어야 한다.

**A2 설정 목표 인라인 표** — 삭제 후 포커스가 사라진 행에 남으면 안 된다
(`activeElement === body`면 실패). 기존 `goalSaveStatus`의
`role="status" aria-live="polite"`를 재사용해 한 동작에 한 번만 알린다.
요청 중 버튼 비활성화로 중복 DELETE를 막고, **서버 확인 전에 행을 낙관적으로
지우지 않는다**(롤백 없는 즉시 삭제는 결함). 회원 탈퇴는 스킨을 바꿔도 색
이외의 위험 신호(문구·아이콘)를 유지한다.

**C1/C2** — 재스킨 대상 버튼과 `target_hours` 입력이 360px에서 44×44px 이상을
유지해야 한다(제거하는 인라인 `<style>`가 현재 폭을 강제하고 있다).
`mypage.html`에 포함되는 `usergoal_list.html`의 버튼도 같은 패스에 포함한다.
- 계획 브라우저 증거 — 360px·760px·1280px 렌더, 콘솔 점검,
  변경 JS `node --check`, 드래그·모달 키보드 경로, 대비 확인
- 사후 판정 (WED / BIR) — *미작성*
- Quality Verification Lead 완료 판단 — *미작성*
