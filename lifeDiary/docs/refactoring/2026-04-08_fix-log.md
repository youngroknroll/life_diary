# 2026-04-08 Fix Log

> 코드 리뷰 기반 수정 작업 기록 | 16/23 완료

## 변경 파일 (9개)

| 파일 | 수정 항목 |
|------|-----------|
| apps/core/utils.py | `calculate_goal_percent()` 헬퍼 추가 (#8-9) |
| apps/dashboard/views.py | slot_indexes 검증 (#2), memo 길이 제한 (#6), exception leak 제거 (#3), logging 추가 |
| apps/stats/feedback.py | 목표 계산 버그 수정 (#10), bare except→logging (#14), 하드코딩 태그→상수 (#20) |
| apps/stats/logic.py | N+1→단일 annotate 쿼리 (#7), goal-calc 중복 제거 (#9), Count import 추가 |
| apps/tags/views.py | exception leak 4곳 제거 (#4), logging 추가 |
| apps/users/models.py | target_hours MinValueValidator(0) (#19) |
| apps/users/views.py | .get()→get_object_or_404 4곳 (#1), delete 뷰 데코레이터 (#11), goal-calc→헬퍼 (#8) |
| lifeDiary/settings/dev.py | SecurityMiddleware↔WhiteNoise 순서 교정 (#12) |
| lifeDiary/settings/prod.py | SSL_REDIRECT, HSTS, cookie secure 헤더 6개 추가 (#5) |

## 수정 상세

### CRITICAL (5/5)

**#1 IDOR — users/views**
```python
# Before
goal = UserGoal.objects.get(pk=pk, user=request.user)
# After
goal = get_object_or_404(UserGoal, pk=pk, user=request.user)
```
4곳: usergoal_update, usergoal_delete, usernote_update, usernote_delete

**#2 slot_indexes 검증 — dashboard/views**
```python
if not isinstance(slot_indexes, list) or not all(
    isinstance(i, int) and 0 <= i < TOTAL_SLOTS_PER_DAY for i in slot_indexes
):
    return error_response(...)
```

**#3-4 exception leak — dashboard/views, tags/views (6곳)**
```python
# Before
except Exception as e:
    return error_response(f"오류: {str(e)}", ...)
# After
except Exception:
    logging.getLogger(__name__).exception("오류 컨텍스트")
    return error_response("오류가 발생했습니다.", ...)
```

**#5 프로덕션 보안 헤더 — settings/prod.py**
```python
SECURE_SSL_REDIRECT = True
SECURE_HSTS_SECONDS = 31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
```

### HIGH (7/7)

**#6 memo 길이 제한 — dashboard/views**
```python
if memo and len(memo) > 500:
    return error_response("메모는 500자를 초과할 수 없습니다.", "MEMO_TOO_LONG")
```

**#7 N+1 쿼리 — stats/logic (31쿼리→1쿼리)**
```python
# Before: for day_index in range(total_days): TimeBlock.objects.filter(...).count()
# After
daily_counts = dict(
    TimeBlock.objects.filter(user=user, date__range=[...])
    .values_list("date").annotate(cnt=Count("id")).values_list("date", "cnt")
)
recorded_blocks = daily_counts.get(current_date, 0)
```
fill_empty_slots_monthly, fill_empty_slots_analysis 두 메서드 모두 적용

**#8-9 goal-calc 중복 제거 — core/utils + users/views + stats/logic**
```python
# core/utils.py — 새 헬퍼
def calculate_goal_percent(goal, daily_stats=None, weekly_stats=None, monthly_stats=None):
    # period별 actual 조회 → percent 계산
    return actual, percent

# users/views.py mypage — 45줄→5줄
for goal in goals:
    goal.actual, goal.percent = calculate_goal_percent(goal, ...)

# stats/logic.py get_stats_context — 32줄→4줄
for goals in [user_goals_daily, user_goals_weekly, user_goals_monthly]:
    for goal in goals:
        goal.actual, goal.percent = calculate_goal_percent(goal, ...)
```

**#10 월간 목표 계산 버그 — stats/feedback**
```python
# Before: remain = goal.target_hours * context["monthly_stats"]["total_days"] - goal.actual
# After:  remain = goal.target_hours - goal.actual
```
사용자가 이미 월간 총 시간으로 입력 → total_days 곱셈 제거

**#11 delete 뷰 데코레이터 — users/views**
```python
@login_required
@require_http_methods(["GET", "POST"])
def usergoal_delete(request, pk):
```
usergoal_delete, usernote_delete 두 곳

**#12 미들웨어 순서 — settings/dev.py**
```python
# Before: WhiteNoise → SecurityMiddleware
# After:  SecurityMiddleware → WhiteNoise
```

### MEDIUM (4/8)

**#14 bare except — stats/feedback**
```python
# Before: except Exception: pass
# After:  except Exception: logger.exception("주간 비교 피드백 생성 중 오류")
```

**#19 target_hours 최소값 — users/models**
```python
target_hours = models.FloatField(validators=[MinValueValidator(0)])
```

**#20 하드코딩 태그명 — stats/feedback**
```python
# Before: tag["name"] not in ["미분류", "휴식"]
# After:  tag["name"] not in [UNCLASSIFIED_TAG_NAME, SLEEP_TAG_NAME]
```
3곳 모두 상수로 교체

## 미수정 (7개)

| # | Issue | 사유 |
|---|-------|------|
| 13 | StatsCalculator mixed concerns | 구조 리팩토링 필요, 별도 작업 |
| 15 | is_default flip permission gap | 정책 결정 필요 |
| 16 | obscure time header formula | 가독성 개선, 낮은 위험 |
| 17 | `\|` queryset union → Q() | 동작 동일, 스타일 개선 |
| 18 | console.error in production | JS 파일, 별도 작업 |
| 21 | TextField max_length in PG | PG에서 무시됨, 낮은 위험 |
| 22 | DIRS includes BASE_DIR | 보안 낮음, 별도 검토 |
| 23 | no auto-login after signup | UX 개선, 별도 작업 |

## 검증

- `py_compile` 9개 파일 전부 syntax OK
- Django 런타임 검증: 배포 전 `python manage.py check --deploy` 실행 필요
